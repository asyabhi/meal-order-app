import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function main() { try { const user = await prisma.user.findFirst(); const coke = await prisma.menuItem.findFirst({ where: { name: "Coke" }}); console.log("Coke:", coke); const check = await prisma.order.findFirst({ where: { userId: user?.id, date: "2026-03-20", menuItem: { category: "Drinks" } }}); console.log("Check:", check); if(!check) { await prisma.order.create({ data: { userId: user.id, menuItemId: coke.id, date: "2026-03-20" }}); console.log("Order Created!"); }} catch(e) { console.error("ERR", e); } finally { await prisma.$disconnect(); } } main();
